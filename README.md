# Instrucciones para ejecutar el proyecto
1. Clona el repositorio.
2. Instala las dependencias:
   ```bash
   npm install

 Comentarios sobre la prueba:

-Tiempo: Me ha llevado algo mas de una hora devido a que me hubiera gustado usar el siguiente codigo ( que es la tendencia actual para estos casos ) para recoger los datos de los inputs:

 <div *ngIf="loginForm.get('email')!.invalid && loginForm.get('email')!.touched"> 
 <div *ngIf="loginForm.get('password')!.invalid && loginForm.get('password')!.touched">

 aunque funcionaba perfectamente me daba un error en consola asi que decidi usar dos funcionalidades.

 -Diseño. Mis habilidades en diseño superan a lo visto en la prueba, pero he preferido priorizar la funcionalidad y reactividad en el proyecto. Me he basado en un formulario de su pagina web.  



